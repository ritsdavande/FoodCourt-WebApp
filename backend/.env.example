# Environment Variables for Backend Deployment

# MongoDB Connection String
# Get this from MongoDB Atlas: Database → Connect → Connect your application
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/FoodCourtmern?retryWrites=true&w=majority

# Node Environment
NODE_ENV=production

# Port (Render will override this automatically)
PORT=10000
